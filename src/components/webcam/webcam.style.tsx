import styled from 'styled-components'

export const webcamContainer = styled.video`
    width: 100%;
    height: 100%;
    border-radius: .5em;
    object-fit: none;
    transform: rotateY(180deg);
`